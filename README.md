# Custom vcpkg ports registry

Internal component, do not use. Contact our support via www.id.ee for assistance.

## Update repository:

    vcpkg x-add-version --x-builtin-ports-root=./ports --x-builtin-registry-versions-dir=./versions --all
